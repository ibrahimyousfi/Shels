# 🔑 دليل إعداد Environment Variables في Vercel

## 📋 ما هي Environment Variables؟

Environment Variables هي متغيرات سرية (مثل API Keys) التي يحتاجها التطبيق للعمل، لكن لا يجب أن تكون مرئية في الكود.

في حالتنا، نحتاج إلى:
- `GEMINI_API_KEY` - مفتاح API الخاص بـ Google Gemini 3

---

## 🎯 لماذا نحتاجها في Vercel؟

عندما تنشر التطبيق على Vercel، الكود يعمل على خوادم Vercel (ليس على جهازك).
لذلك، يجب إضافة `GEMINI_API_KEY` في Vercel Dashboard حتى يعمل التطبيق.

---

## 📝 خطوات إضافة Environment Variables في Vercel

### الخطوة 1: افتح Vercel Dashboard

1. اذهب إلى: `https://vercel.com/dashboard`
2. سجل الدخول بحسابك
3. اختر المشروع `Shels` (أو اسم المشروع الخاص بك)

---

### الخطوة 2: اذهب إلى Settings

1. في صفحة المشروع، اضغط على **"Settings"** (في القائمة العلوية)
2. من القائمة الجانبية، اضغط على **"Environment Variables"**

---

### الخطوة 3: أضف GEMINI_API_KEY

1. ستجد قسم **"Environment Variables"**
2. اضغط على **"Add New"** أو **"Add"**
3. املأ الحقول:
   - **Key**: `GEMINI_API_KEY`
   - **Value**: (ضع مفتاح API الخاص بك هنا)
   - **Environment**: اختر **"Production"** و **"Preview"** و **"Development"** (أو على الأقل Production)

4. اضغط **"Save"**

---

### الخطوة 4: احصل على GEMINI_API_KEY

إذا لم يكن لديك مفتاح API:

1. اذهب إلى: `https://makersuite.google.com/app/apikey`
2. سجل الدخول بحساب Google
3. اضغط **"Create API Key"**
4. انسخ المفتاح
5. الصقه في Vercel (في الخطوة 3)

---

### الخطوة 5: إعادة Deploy

بعد إضافة Environment Variables:

1. اذهب إلى **"Deployments"** في Vercel
2. اضغط على **"Redeploy"** للـ deployment الأخير
3. أو ادفع أي تغيير جديد إلى GitHub (سيتم Deploy تلقائياً)

**ملاحظة مهمة:** بعد إضافة Environment Variables، يجب إعادة Deploy حتى تعمل!

---

## ✅ التحقق من أن Environment Variables موجودة

### في Vercel Dashboard:

1. اذهب إلى Settings → Environment Variables
2. يجب أن ترى `GEMINI_API_KEY` في القائمة
3. تأكد من أن Environment هو **"Production"** (على الأقل)

### اختبار في التطبيق:

1. افتح Production URL (مثل `https://shels.vercel.app`)
2. جرب إدخال GitHub URL
3. اضغط "Start Testing"
4. إذا بدأ التحليل → Environment Variables تعمل ✅
5. إذا ظهرت رسالة خطأ عن API → تحقق من المفتاح

---

## 🚨 مشاكل شائعة وحلولها

### المشكلة 1: "API key not found" أو "Invalid API key"

**الحل:**
1. تحقق من أن `GEMINI_API_KEY` موجود في Vercel
2. تحقق من أن القيمة صحيحة (نسخ كامل)
3. تأكد من إعادة Deploy بعد إضافة المفتاح

---

### المشكلة 2: "Environment variable not set"

**الحل:**
1. تأكد من أن الاسم صحيح: `GEMINI_API_KEY` (حساس لحالة الأحرف)
2. تأكد من أن Environment هو **"Production"**
3. أعد Deploy بعد إضافة المفتاح

---

### المشكلة 3: التطبيق يعمل محلياً لكن لا يعمل على Vercel

**السبب:**
- Environment Variables موجودة في `.env.local` (محلي) لكن غير موجودة في Vercel

**الحل:**
- أضف `GEMINI_API_KEY` في Vercel Dashboard (كما هو موضح أعلاه)

---

## 📸 مثال على الشاشة في Vercel

```
Settings → Environment Variables

┌─────────────────────────────────────────┐
│ Environment Variables                   │
├─────────────────────────────────────────┤
│ Key              │ Value │ Environment │
├─────────────────────────────────────────┤
│ GEMINI_API_KEY   │ ***** │ Production  │
│                  │       │ Preview     │
│                  │       │ Development │
└─────────────────────────────────────────┘

[Add New] button
```

---

## ✅ قائمة التحقق

- [ ] حصلت على `GEMINI_API_KEY` من Google AI Studio
- [ ] فتحت Vercel Dashboard
- [ ] ذهبت إلى Settings → Environment Variables
- [ ] أضفت `GEMINI_API_KEY` مع القيمة الصحيحة
- [ ] اخترت Environment: Production (و Preview و Development)
- [ ] حفظت التغييرات
- [ ] أعدت Deploy المشروع
- [ ] اختبرت التطبيق على Vercel
- [ ] التحليل يعمل بشكل صحيح ✅

---

## 🎯 الخلاصة

**باختصار:**
1. افتح Vercel Dashboard
2. Settings → Environment Variables
3. أضف `GEMINI_API_KEY` مع مفتاح API الخاص بك
4. أعد Deploy
5. اختبر التطبيق

**هذا كل شيء!** 🚀
